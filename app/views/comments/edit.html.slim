h1 Comments#edit
p Find me in app/views/comments/edit.html.slim
.nav.jsutify-content-end
= link_to '一覧', discussion_comment_path, class:'nav-link'

= form_with(model: [@comment.discussion, @comment], local: true) do |f| 
 .form-group
 = f.label :content
 = f.text_area :content, class: 'form-control', id: 'comment_content' 

 .form-action
 = f.submit nil, class: 'btn btn-primary' 
 
 = link_to '戻る', discussion_path(@comment.discussion_id)