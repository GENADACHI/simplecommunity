h1 コメント詳細

.nav.jsutify-content-end
  = link_to '戻る', discussion_path(@comment.discussion_id)
table.table.table-hover
  tbody
    tr
      th= Comment.human_attribute_name(:内容)
      td= @comment.content
    tr
      th= Comment.human_attribute_name(:作成日)
      td= @comment.created_at
    tr
      th= Comment.human_attribute_name(:updated_at)
      td= @comment.updated_at
p.Content: = @comment.present?
= link_to '編集', edit_discussion_comment_path(@discussion, @comment)
= link_to '削除', discussion_comment_path(@discussion, @comment), method: :delete, data: { confirm: '削除します。よろしいですか?' }, class: 'btn btn-danger'
= link_to '返信', new_comment_answer_path(@comment, @answer)